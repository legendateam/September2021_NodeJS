export const COOKIE = {
    nameAccessToken: 'accessToken',
    nameRefreshToken: 'refreshToken',
    // maxAgeRefreshToken it's just for example with cookies
    maxAgeRefreshToken: 1 * 24 * 60 * 60 * 1000,
};
